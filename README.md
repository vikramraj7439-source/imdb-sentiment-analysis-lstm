📌 IMDB Sentiment Analysis using LSTM
🔍 Project Overview

This project builds a deep learning model to classify movie reviews as Positive or Negative using the IMDB dataset.

The model is trained using an LSTM (Long Short-Term Memory) network and achieves strong performance on unseen test data.